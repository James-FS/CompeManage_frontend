# 测试账号和密码

## 数据库测试账号

根据前端模拟数据，已在数据库中添加以下 5 个测试账号：

| 用户名 | 密码 | 角色 | 真实姓名 |
|--------|------|------|---------|
| admin | 123 | 校级管理员 | 校级管理员 |
| yuan | 123 | 院级管理员 | 计算机学院管理员 |
| teacher | 123 | 赛事负责人 | 张老师(赛事负责人) |
| student | 123 | 学生 | 李同学 |
| expert | 123 | 专家 | 王专家 |

## 启动步骤

### 1. 启动后端服务

```bash
cd CompeManage_backend
go run main.go
```

服务将在 `http://localhost:8080` 启动

启动日志会显示：
```
🎉 模拟数据初始化完成！
--------------------------------
校级管理员: admin    密码: 123
院级管理员: yuan     密码: 123
赛事负责人: teacher  密码: 123
学生:     student   密码: 123
专家:     expert    密码: 123
--------------------------------
```

### 2. 启动前端服务

```bash
cd CompeManage_frontend/CompeManage
npm run dev
```

### 3. 测试登录

访问登录页面，使用上述任意账号登录即可。

## 接口对接

登录接口已对接到后端：
- 前端发送请求到：`POST /api/login`
- 传递参数：`{ username: string, password: string }`
- 后端返回：`{ code: 200, msg: "登录成功", data: { token: string, userInfo: {...} } }`

## 注意事项

1. 密码在数据库中已使用 bcrypt 加密存储
2. 每个角色有对应的权限配置
3. 登录成功后会返回 JWT Token，用于后续API请求认证
4. Token 自动保存在 localStorage 中，请求时自动添加到请求头

## 快速测试

在浏览器控制台测试登录：

```javascript
// 在浏览器控制台运行
const response = await fetch('http://localhost:8080/api/login', {
  method: 'POST',
  headers: { 'Content-Type': 'application/json' },
  body: JSON.stringify({ username: 'admin', password: '123' })
})
const data = await response.json()
console.log(data)
```

预期返回：
```json
{
  "code": 200,
  "msg": "登录成功",
  "data": {
    "token": "eyJhbGciOiJIUzI1NiIs...",
    "userInfo": {
      "id": 1,
      "username": "admin",
      "realname": "校级管理员",
      "role": "school_admin"
    }
  }
}
```
